#' @param select A vector of column names or positions to keep. The rest of the
#'        columns are not read. The order that the columns passed determines the
#'        order of the columns in the result.
